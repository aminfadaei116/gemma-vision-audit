# PyTorch with CUDA 12.1 support (Linux/cloud VM)
--index-url https://download.pytorch.org/whl/cu121
torch
torchvision

# Core
transformers
datasets
accelerate
peft
trl

# Data & images
pillow
tqdm
pandas

# Evaluation
rouge-score
bert-score

# LLM-based label conversion (optional — only needed for --mode llm)
openai

# Notebook
jupyter
ipywidgets
matplotlib
